try{
    console.log(a);

}catch(error){
    console.log("a is not Defined");
}
/*
Lecture # 66
    Topic : try Catch in JavaScript

    try:
        try is used to test code that may cause an error.

    catch:
        catch handles the error if an error happens inside try.
*/