/*
Lecture # 29
    Topic : for in Loop in JavaScript

    5. for...in Loop :
        Used for object properties (keys).

    Syntax :
        const user = {
        name: "Ali",
        age: 22
        };

    for (let key in user) {
        console.log(key, user[key]);
        }

*/