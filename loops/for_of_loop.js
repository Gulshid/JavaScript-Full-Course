const Marks = [10, 20, 30, 40, 50];

// Use the for - of loop
for (let x of Marks)
{
    console.log(x);
}

/*
Lecture # 28
    Topic : for - of loop in JavaScript

    for...of Loop:
        Used for arrays, strings, iterable objects.

    Syntax :

    const a = [1,2,3];
        for (let arr of a) {
            console.log(arr);
            }

*/